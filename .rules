# u-forge.ai — LLM Rules Entrypoint

Routing guide and standing bulletin board. Read this file first for every task.
Rule files live in `.rulesdir/` — read only the ones that apply to your task.

---

## ⚠️ Anti-Patterns — Read Before Writing Anything

These are active failure modes observed in this codebase. Violating them wastes
cycles and produces PRs that must be reverted.

### 1. Code defaults first — env vars are overrides, not requirements

A user must be able to clone this repo and run `cargo run --manifest-path crates/u-forge-core/Cargo.toml --example cli_demo` with
**no environment variables set** and have it work (degrading gracefully when no
Lemonade Server is present). Env vars like `LEMONADE_URL` exist only to override
a non-standard location or port.

- In code, always try a **sensible default first** (e.g. `http://localhost:13305/api/v1`),
  then fall back to the env var if the default is unreachable or unset.
  `EmbeddingManager::try_new_auto` and `lemonade::resolve_lemonade_url()` already
  implement this pattern — follow them, never bypass them.
- Never write application code that calls `env::var(...).unwrap()` or
  `.expect("set LEMONADE_URL")` at the top level. An unset env var must never
  be a hard error for core functionality.
- The same applies to tests: `cargo test -- --test-threads=1` must pass with zero
  env vars. Use `crate::test_helpers::lemonade_url().await` (probes localhost first,
  then `LEMONADE_URL`) and silently skip — never fail — when no server is reachable.

### 2. Check the live Lemonade API before making capability decisions

Before assuming what models or hardware are available, **fetch the actual server
state**. `SystemInfo::fetch(url)` and `LemonadeServerCatalog::discover(url)` exist
for exactly this reason — use them. From the catalog, build a `ModelSelector` to
pick the best available models for each capability. The `cli_demo` pattern (fetch →
select → build providers → degrade gracefully) is the canonical example. Do not
hardcode model IDs, device presence, or capability flags without a live catalog
check to back them up.

### 3. Prescriptive plans belong in feature or bug files — not in shared docs

Implementation plans, step-by-step task lists, phased roadmaps, PR checklists, and
any other prescriptive or future-state content must live **only** in a `feature_*.md`
or `bug_*.md` file at the workspace root. These files are purpose-built for driving
active development and are expected to be prescriptive.

`.rules`, `.rulesdir/*.mdc`, `ARCHITECTURE.md`, `CLAUDE.md`, and `README.md` must
remain descriptive — they describe what **is**, not what **will be** or **should be
done next**. If you find yourself writing a plan, a TODO list, a phase checklist, or
a "remaining work" section in any of those files, stop and put it in the appropriate
`feature_*.md` or `bug_*.md` instead.

**What counts as prescriptive (belongs in feature/bug files):**
- Ordered implementation steps or phases
- "Next:" / "Remaining:" / "TODO:" sections
- PR descriptions or checklists
- Anything with ✅/⏳ progress tracking
- Architectural decisions not yet reflected in the code

**What belongs in shared docs:**
- How the code currently works
- Where to find things
- Invariants and constraints the code already enforces

### 4. Source code is the source of truth — docs are indexes, not mirrors

Every `.rs` file in `src/` and `examples/` is thoroughly commented. `.md` files
and `.mdc` rules exist to tell you **where to look and why it matters**, not to
repeat what the code already says. If you find yourself writing a code example or
struct definition in a doc that already exists verbatim in source, replace it with
a pointer (`see src/lemonade/rerank.rs — LemonadeRerankProvider`). Keep docs as
navigational aids.

---

## Current Project State (snapshot)

Working prototype. Multi-crate Cargo workspace under `crates/`. SQLite + FTS5 storage,
sqlite-vec ANN search, catalog-driven provider selection, unified inference queue
(`InferenceQueue`), hybrid search pipeline, bulk graph access (`get_all_edges`,
`get_nodes_paginated`). Interactive RAG chat REPL (`cli_chat`). Native GPUI graph
canvas renders real graph data with pan/zoom and LOD. No active bugs.

UI crates in progress (see `feature_UI.md`):
- `u-forge-graph-view` — `GraphSnapshot` + `build_snapshot()`, force-directed layout (grid-cell bucketed O(N) repulsion), R-tree spatial index, viewport culling, `rebuild_spatial_index()`.
- `u-forge-ui-traits` — `DrawCommand` / `Viewport` / `generate_draw_commands()` rendering pipeline (culling, LOD, type-color). Framework-agnostic.
- `u-forge-ui-gpui` — GPUI 0.2.2 canvas app. `AppView` root view: 28 px menu bar (File > Save / Ctrl+S, Nodes toggle / Ctrl+B via `ToggleSidebar`) + horizontal body (`overflow_hidden()` flex row): optional 220 px `TreePanel` sidebar + vertical workspace (30/70 editor placeholder / graph canvas). `SelectionModel` entity shared by `TreePanel` and `GraphCanvas`; `cx.observe()` keeps both in sync — tree clicks pan the canvas, canvas clicks highlight the tree. `suppress_pan` flag prevents camera jump on canvas-originated selection. `GraphCanvas` holds `canvas_bounds: Arc<RwLock<Bounds<Pixels>>>` updated each paint frame; all `window.paint_*` calls add `bounds.origin` offset and `overflow_hidden()` clips output to the pane. DB path from `[storage] db_path` in `u-forge.toml` (default `./data/db`), pan/zoom/LOD, node select + drag, positions autosaved on each drag-end. Run: `cargo run -p u-forge-ui-gpui`.
- `u-forge-core` — `node_positions` table added to SQLite schema; `KnowledgeGraph::save_layout()` / `load_layout()` persist and restore canvas positions. `build_snapshot()` applies saved positions after force-directed layout, so new nodes get computed positions and known nodes restore their last user-placed position.

Recent quality improvements:
- `load_model()` now accepts `already_loaded: &[String]` — if the model ID is in the slice the HTTP round-trip to `/api/v1/load` is skipped entirely. Callers extract these IDs from `catalog.loaded` (already fetched by `LemonadeServerCatalog::discover`). `ProviderFactory::build` threads this parameter through all capability branches. Both CLI demos extract the loaded-model list from the catalog and fire all provider builds concurrently via `futures::future::join_all` / `tokio::join!`, eliminating sequential load waits on warm servers.
- `embed_all_chunks` is now incremental — uses `get_unembedded_chunks` / `get_unembedded_chunks_hq` (LEFT JOIN on `chunks_vec`) instead of re-scanning all objects.
- Hybrid search diagnostic blocks demoted from `info!` to `debug!` — no more log spam in production; `RUST_LOG=u_forge_core::search=debug` reveals full pipeline state.
- `KnowledgeGraphStorage` now uses `parking_lot::Mutex` — all 24 `.lock().unwrap()` calls removed.
- `ObjectType` enum deleted from `types.rs` — it was dead code; `ObjectMetadata.object_type` is `String` throughout, and `ObjectBuilder` already embeds correct string literals.
- `WeightedEmbedDispatcher::worker_count()` removed — `InferenceQueue` exposes `embedding_worker_count()` for callers. `WeightedWorkerSlot::name` and `idle` fields annotated with intent comments (retained for future diagnostics).
- HTTP timeouts added to all Lemonade HTTP clients — `LemonadeHttpClient::new()` sets 30s request / 5s connect; lightweight discovery clients (`system_info`, `catalog`) use 5s / 2s. Prevents indefinite blocking when Lemonade Server hangs.
- Token estimation changed from `len / 4` to `len.div_ceil(3)` — conservative 3-char/token ratio better matches dense TTRPG prose. `split_text()` in `text.rs` uses the same ratio (`MAX_CHUNK_TOKENS * 3` chars per chunk).
- `EdgeType` collapsed from single-variant enum (`Custom(String)`) to transparent newtype struct. Use `EdgeType::new(s)` everywhere — `Custom(s)` no longer exists.
- `ObjectId` and `ChunkId` promoted from type aliases to newtype structs (`#[serde(transparent)]`). The compiler now rejects passing a `ChunkId` where an `ObjectId` is expected. Construct with `::new_v4()`; parse with `::parse_str(s)`.
- 9 clippy warnings resolved: `while let` loop, boxed large enum variant (`ProviderSlot::Tts`), `#[derive(Default)]` on `KokoroVoice`, transmute type annotation for sqlite-vec FFI, doc formatting. Edge metadata JSON parse failures now emit `debug!()` instead of silently swallowing errors.

The core library has been fully refactored (hardware abstraction layer removed):
- `hardware/` directory deleted — `NpuDevice`, `GpuDevice`, `CpuDevice`, `DeviceWorker`
  trait, and `HardwareBackend` enum no longer exist.
- `lemonade/device_factory.rs`, `lemonade/model_limits.rs`, `lemonade/registry.rs`,
  `lemonade/stack.rs` deleted. `LemonadeModelRegistry`, `ModelRole`, `LemonadeStack`,
  `EmbeddingManager`, and `TranscriptionManager` no longer exist.
- Replaced by: `lemonade/catalog.rs` (`LemonadeServerCatalog`), `lemonade/selector.rs`
  (`ModelSelector`, `SelectedModel`, `QualityTier`), `lemonade/duplicate_guard.rs`
  (`DuplicateGuard`), `lemonade/provider_factory.rs` (`ProviderFactory`, `Capability`,
  `BuiltProvider`).
- `ai/embeddings.rs` and `ai/transcription.rs` are trait-only files; implementations
  live in `lemonade/embedding.rs` and `lemonade/transcription.rs`.
- `InferenceQueueBuilder` now wires providers via `.with_provider(BuiltProvider)` /
  `.with_providers(Vec<BuiltProvider>)`. `ProviderFactory::build()` constructs them.
- `AppConfig` includes `[chat]` with per-device model overrides (`[chat.gpu]`, `[chat.npu]`) and `[storage]` with `db_path` (default `./data/db`). `ModelConfig.load_params` replaces the old `context_limits: HashMap`.

---

## Always Read First

> **`.rulesdir/project-structure.mdc`**

Read for every task. Module map, current architecture, migration status, key
integration points, hardware capability table.

---

## Task-Based Routing

### Building, running, or setting up the environment
→ **`.rulesdir/environment-config.mdc`**
→ **`.rulesdir/development-workflow.mdc`**

`env.sh` is **optional** — no longer required for compilation. `cargo build` and
`cargo test` work with no env vars. See these files for Lemonade Server setup and
the `LEMONADE_URL` auto-discovery order.

---

### Writing or modifying Rust source (`src/**/*.rs`, `examples/*.rs`)
→ **`.rulesdir/rust-patterns.mdc`**

Error handling (`anyhow`), `EmbeddingProvider` trait (do NOT bypass it),
`TranscriptionProvider` trait, `InferenceQueue` usage, `ObjectBuilder`, async
anti-patterns, storage patterns, tracing instrumentation.

---

### Adding or wiring a new AI capability (embedding, reranking, TTS, STT, LLM)
→ **`.rulesdir/rust-patterns.mdc`** (EmbeddingProvider / trait sections)
→ **`.rulesdir/development-workflow.mdc`** (provider addition workflow)

New providers implement the relevant trait (`EmbeddingProvider` or
`TranscriptionProvider`). For queue-dispatched work, build via `ProviderFactory` and
register with `InferenceQueueBuilder::with_provider`. Do NOT call provider HTTP
clients directly from business logic. Do NOT use `Mutex<T>` in async paths.
There is no `DeviceWorker` trait — that abstraction was removed in the hardware
refactor.

---

### Writing, running, or debugging tests
→ **`.rulesdir/testing-debugging.mdc`**
→ **`.rulesdir/rust-patterns.mdc`** (testing patterns section)

Re-read anti-pattern §1 above before writing any test. Every test gets its own
`TempDir`. Mock providers for unit tests; `lemonade_url().await` skip-guard for
integration tests.

---

### Schema files (`defaults/schemas/*.schema.json`)
→ **`.rulesdir/schema-system.mdc`**
→ **`.rulesdir/json-data-formats.mdc`**

`add_` prefix convention, `SchemaIngestion` stripping, property types, the 13
built-in TTRPG types.

---

### Data files (`defaults/data/*.json`) or the import pipeline
→ **`.rulesdir/json-data-formats.mdc`**

JSONL node/edge format, two-pass import, deduplication, edge resolution.

---


## Quick Reference: File → Topic

| File | Primary Topics |
|---|---|
| `project-structure.mdc` | Architecture, module map, migration status, integration points |
| `development-workflow.mdc` | Build/run commands, provider addition, Lemonade setup |
| `environment-config.mdc` | Env vars, auto-discovery order, optional `env.sh` |
| `rust-patterns.mdc` | Error handling, traits, async, storage, ObjectBuilder |
| `schema-system.mdc` | Schema format, validation, property types |
| `json-data-formats.mdc` | JSONL import format, two-pass pipeline |
| `testing-debugging.mdc` | TempDir setup, mock providers, skip guards, CI commands |
| `ARCHITECTURE.md` | Full module map, SQLite schema, hardware design, design decisions |