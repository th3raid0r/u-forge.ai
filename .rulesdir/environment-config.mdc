---
globs: env.sh,dev.sh,*.toml,*.yaml,*.yml,config.*
---

# Environment and Configuration

## env.sh is optional

`env.sh` no longer sets compiler variables — bundled SQLite compiles with any
standard C compiler on PATH. Source it only if you want the convenience path
exports.

## Environment Variables

```bash
# All optional — the app runs with nothing set.
export LEMONADE_URL="http://localhost:13305/api/v1"  # Only needed for non-default server URL
export UFORGE_SCHEMA_DIR="./defaults/schemas"        # Override schema directory
export UFORGE_DATA_FILE="./defaults/data/memory.jsonl" # Override data file
export RUST_LOG=info                                 # Log verbosity

# Direct `cargo test` integration control (canonical `make test` overrides this)
export UFORGE_INTEGRATION_TESTS=require  # Panic instead of skip when server unreachable
export UFORGE_INTEGRATION_TESTS=skip     # Force-skip even when a server is present
export UFORGE_SKIP_EMBEDDED_LEMONADE=1   # Skip automatic runtime provisioning
```

### Why env vars are overrides, not requirements

`cargo run -p u-forge-ui-gpui` must work with zero env vars set. An unset env
var must never be a hard error for core functionality.
`lemonade::resolve_lemonade_url()` encodes this — never `env::var(...).unwrap()`
at the top level.

### Module-specific logging

```bash
export RUST_LOG=u_forge_core::graph::storage=debug,u_forge_core::ai::embeddings=info

# Lemonade discovery plus redacted stdout/stderr from the owned lemond child.
export RUST_LOG=info,u_forge_core::lemonade::management=debug,u_forge_core::lemonade::embedded::lemond=debug
```

`RUST_LOG` controls u-forge's tracing filter. Embeddable Lemonade has its own
`log_level` setting in `$XDG_CACHE_HOME/u-forge/lemonade/config.json`, or
`~/.cache/u-forge/lemonade/config.json` when `XDG_CACHE_HOME` is unset. Models
and downloaded backends live under that same cache root; model paths never
fall back to the global Hugging Face cache.
Set that JSON field to `"debug"` when Lemonade's default informational child
events are insufficient; u-forge forwards those lines only when the dedicated
`lemond` tracing target above is enabled and redacts both runtime credentials.

## Path Resolution

### Schema Directory (checked in order)
1. `$UFORGE_SCHEMA_DIR`
2. `./defaults/schemas`
3. `../defaults/schemas`

### Data File (checked in order)
1. `$UFORGE_DATA_FILE`
2. `./defaults/data/memory.jsonl`
3. `../defaults/data/memory.jsonl`

## Database Location

`KnowledgeGraph::new(db_path)` opens (or creates) a SQLite database at
`<db_path>/knowledge.db`. The directory is created automatically.

`[storage].embedding_dimensions` and
`[storage].high_quality_embedding_dimensions` configure the two sqlite-vec
lanes (defaults: 768 and 4096). Dimensions are persisted in database metadata;
changing either value requires rebuilding the database.

The desktop app uses a persistent database. Set `UFORGE_DATA_FILE` /
`UFORGE_SCHEMA_DIR` to customise import sources.

---

## Lemonade Server

Lemonade Server provides all AI inference (embedding, reranking, chat, STT,
TTS) via an OpenAI-compatible HTTP API. On Ubuntu x64, building the desktop UI
downloads the checksum-pinned Embeddable Lemonade release into `target/` and
places its runtime beside the application executable. The build adds the
`reasoning` catalog label to built-in Gemma 4 GGUF models before installation.
If provisioning is skipped or unavailable, AI features degrade gracefully and
the knowledge graph remains usable.

### Setup

```bash
# Install (Linux via snap)
sudo snap install lemonade-server

# Pull required models
lemonade-server pull embed-gemma-300m-FLM      # embeddings (NPU, 0.62 GB) — default
lemonade-server pull whisper-v3-turbo-FLM      # transcription (NPU, 1.55 GB)
lemonade-server pull bge-reranker-v2-m3-GGUF   # reranking (GPU/CPU)

# Start
lemonade-server serve

# Verify — also shows which models are currently loaded in RAM
curl http://localhost:13305/api/v1/health
```

Cross-platform Lemonade install: Linux → `snap`; macOS → `.pkg` from GitHub
releases; Windows → `.exe` installer.

### Canonical test ownership

`make test` requires and launches the checksum-pinned Embeddable Lemonade
artifact once, exports its private URL and credentials to every test binary,
then unloads models and terminates the server's complete process group. It
ignores `LEMONADE_URL`, `UFORGE_LEMOND_PATH`, and
`UFORGE_SKIP_EMBEDDED_LEMONADE` so a developer's external server cannot change
the tested version. The application and test runner reuse the same XDG-scoped
model/backend cache, so tests do not create a second download tree. Direct
`cargo test` retains the auto-skip controls below for narrow local iteration.

### URL Resolution Order

`lemonade::resolve_lemonade_url()`:
1. `LEMONADE_URL` env var — normalized and accepted as-is
2. `GET http://localhost:13305/v1/health` — probed with 2 s timeout
3. `GET http://127.0.0.1:13305/v1/health` — explicit IPv4 loopback

Returns `None` when no server is found. Setting `LEMONADE_URL` is optional when
Lemonade runs on its default port.

### Endpoints in Use

| Endpoint | Method | Provider / Purpose |
|---|---|---|
| `/api/v1/models` | GET | `LemonadeServerCatalog::discover()` |
| `/system-info` | GET | `LemonadeServerCatalog::discover()` — installed backends |
| `/api/v1/health` | GET | `LemonadeServerCatalog::discover()` + manual verification |
| `/api/v1/load` | POST | `load_model()` — load a model before inference |
| `/api/v1/embeddings` | POST (JSON) | `LemonadeProvider` |
| `/api/v1/audio/transcriptions` | POST (multipart) | `LemonadeTranscriptionProvider` (no GPU lock) / `LemonadeSttProvider` (GPU-managed) |
| `/api/v1/audio/speech` | POST (JSON) | `LemonadeTtsProvider` |
| `/api/v1/chat/completions` | POST (JSON) | `LemonadeChatProvider` |
| `/api/v1/reranking` | POST (JSON) | `LemonadeRerankProvider` |

### Why two transcription providers

`LemonadeTranscriptionProvider` has no GPU resource management — suitable for
NPU FLM whisper and standalone use. `LemonadeSttProvider` integrates with
`GpuResourceManager` to prevent contention when LLM inference runs concurrently
on the same GPU. Both are constructed via `ProviderFactory::build` with
`Capability::Transcription`. Choice is based on whether GPU contention is
possible, not ergonomic preference.

---

## Troubleshooting

| Problem | Fix |
|---|---|
| `Lemonade Server not available` | Run `lemonade-server serve` — `localhost:13305` is auto-discovered |
| `lemonade-server pull` fails | Check disk space; retry; verify network access |
| Schema not found | Check `$UFORGE_SCHEMA_DIR`, verify `.json` files exist |
| Dimension mismatch | Ensure each embedding model matches its configured vector lane; rebuild the database after changing dimensions |
| Whisper not loaded in RAM | First transcription call triggers load; subsequent calls are fast. `GET /api/v1/health` shows loaded models |
| Transcription error: "Could not open audio data" | Audio bytes empty or not a valid WAV/MP3/OGG |
