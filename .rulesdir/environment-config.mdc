---
globs: env.sh,dev.sh,*.toml,*.yaml,*.yml,config.*
---

# Environment and Configuration

## Current Setup (Post-SQLite Migration)

`env.sh` is **optional** after the Phase 2 migration. It no longer sets compiler
variables — bundled SQLite compiles with any standard C compiler. Source it only if
you want the convenience path exports.

```bash
source env.sh   # optional — sets UFORGE_* path variables
```

> **Before Phase 2** `env.sh` was required to set `CC=gcc-13` for RocksDB.
> That requirement is gone. Plain `gcc` / `g++` (any version) is sufficient for
> the `openssl-sys` transitive dependency brought in by `reqwest`.

## Environment Variables

### Build-time (no longer required)
```bash
# CC / CXX — no longer need to be set to gcc-13.
# Any C/C++ compiler found on PATH works.
```

### Runtime / Optional
```bash
export LEMONADE_URL="http://localhost:8000/api/v1"  # Lemonade Server for embeddings
export UFORGE_SCHEMA_DIR="./defaults/schemas"       # Override schema directory
export UFORGE_DATA_FILE="./defaults/data/memory.json" # Override data file
export RUST_LOG=info                                # Log verbosity
```

### Module-specific logging
```bash
export RUST_LOG=u_forge_ai::storage=debug,u_forge_ai::embeddings=info
```

## Path Resolution

### Schema Directory (checked in order)
1. `$UFORGE_SCHEMA_DIR`
2. `./defaults/schemas`
3. `../defaults/schemas`

### Data File (checked in order)
1. `$UFORGE_DATA_FILE`
2. `./defaults/data/memory.json`
3. `../defaults/data/memory.json`

## Lemonade Server Configuration (Phase 1 — active)

Lemonade Server provides embeddings (and future reranking / chat) via an
OpenAI-compatible HTTP API.  u-forge.ai does **not** fall back to a local model if
Lemonade is unavailable — it returns a clear error with instructions.

```bash
# Install on Linux (snap)
sudo snap install lemonade-server

# Pull required models
lemonade-server pull embed-gemma-300m-FLM    # embeddings (NPU, 0.62 GB) — default embedding model
lemonade-server pull whisper-v3-turbo-FLM    # transcription (NPU, 1.55 GB) — default transcription model
lemonade-server pull bge-reranker-v2-m3      # reranking (Phase 4, not yet integrated)

# Start the server
lemonade-server serve

# Verify health (also shows which models are currently loaded in RAM)
curl http://localhost:8000/api/v1/health

# Point u-forge.ai at it
export LEMONADE_URL="http://localhost:8000/api/v1"
```

`EmbeddingManager::try_new_auto(None, None)` resolves the URL in this order:
1. The `lemonade_url` argument (if `Some`)
2. `LEMONADE_URL` environment variable
3. Hard error — no silent fallback

`TranscriptionManager::try_new_auto(None, None)` uses the **same** resolution order
and is **synchronous** (no probe request at construction). The default transcription
model is `whisper-v3-turbo-FLM`. Errors surface only when `transcribe()` is called.

### Lemonade API Endpoints in Use

| Endpoint | Method | Provider | Phase |
|---|---|---|---|
| `/api/v1/embeddings` | POST (JSON) | `LemonadeProvider` (`embeddings.rs`) | ✅ Phase 1 |
| `/api/v1/audio/transcriptions` | POST (multipart) | `LemonadeTranscriptionProvider` (`embeddings.rs`) | ✅ Phase 1 |
| `/api/v1/audio/transcriptions` | POST (multipart) | `LemonadeSttProvider` (`lemonade.rs`, GPU-managed) | ✅ Phase 1 |
| `/api/v1/audio/speech` | POST (JSON) | `LemonadeTtsProvider` (`lemonade.rs`) | ✅ Phase 1 |
| `/api/v1/chat/completions` | POST (JSON) | `LemonadeChatProvider` (`lemonade.rs`) | ✅ Phase 1 |
| `/api/v1/reranking` | POST (JSON) | *(not yet implemented)* | ⏳ Phase 4 |
| `/api/v1/models` | GET | `LemonadeModelRegistry` (`lemonade.rs`) | ✅ Phase 1 |
| `/api/v1/health` | GET | *(manual verification only)* | — |

> **Note on the two transcription providers:** `LemonadeTranscriptionProvider` in
> `ai/embeddings.rs` is a simple standalone provider with no GPU resource management —
> suitable for one-off use and testing. `LemonadeSttProvider` in `lemonade.rs`
> integrates with `GpuResourceManager` to prevent contention when LLM inference is
> running concurrently on the same GPU. Use `LemonadeSttProvider` (via
> `LemonadeStack`) in production.

## Database Location

`KnowledgeGraph::new(db_path)` opens (or creates) a SQLite database at
`<db_path>/knowledge.db`. The directory is created automatically if it does not
exist.

```bash
# Default in CLI demo: a TempDir is used (ephemeral).
# For a persistent database, pass an explicit path:
cargo run --example cli_demo
# or set UFORGE_DATA_FILE / UFORGE_SCHEMA_DIR to customise import sources.
```

## Planned Configuration (Phase 3 — config.toml)

```toml
[server]
host = "127.0.0.1"
port = 3000

[lemonade]
url = "http://localhost:8000/api/v1"
embedding_model = "nomic-embed-text"
rerank_model = "bge-reranker-v2-m3"

[database]
path = "data/knowledge.db"

[search]
default_fts_limit = 10
default_semantic_limit = 10
rerank_enabled = true
```

## Build Commands

```bash
cargo build                           # Build (first time ~30s for bundled SQLite)
cargo test                            # Run all tests (no server required)
cargo run --example cli_demo          # Run demo with Foundation data
RUST_LOG=debug cargo test -- --nocapture  # Tests with debug logging
```

## Cross-Platform

| OS | C compiler needed | Lemonade install |
|---|---|---|
| Ubuntu / Debian | `apt install gcc` (usually pre-installed) | `snap install lemonade-server` |
| macOS | Xcode CLT (`xcode-select --install`) | `.pkg` from GitHub releases |
| Windows | MSVC (Visual Studio Build Tools) or MinGW | `.exe` installer from releases |

## Troubleshooting

| Problem | Fix |
|---|---|
| `openssl-sys` build error | Install `pkg-config libssl-dev` (Ubuntu) or `openssl` (macOS brew) |
| `Lemonade Server not available` error | Run `lemonade-server serve` and set `LEMONADE_URL` |
| `lemonade-server pull` fails | Check disk space; retry; verify network access |
| Schema not found | Check `$UFORGE_SCHEMA_DIR`, verify `.json` files exist |
| Database locked | Only one process should open the same `knowledge.db` at a time |
| Dimension mismatch (future sqlite-vec) | Ensure embedding model matches vector column dimensions |
| Transcription returns error immediately | Model not pulled — run `lemonade-server pull whisper-v3-turbo-FLM` |
| Transcription error: "Could not open audio data" | Audio bytes are empty or the file is not a valid audio format |
| Whisper not loaded in RAM | First transcription call triggers model load; subsequent calls are fast. Check `GET /api/v1/health` to see which models are in RAM. |